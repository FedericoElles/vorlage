<amp-img class="c-image {{class}}" 
         layout="responsive" 
         srcset="{{image.srcset}}"
         src="{{image.file.url}}" 
         alt="{{description}}" 
         height="{{image.file.details.image.height}}" 
         width="{{image.file.details.image.width}}"></amp-img>
